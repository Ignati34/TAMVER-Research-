# Accessibility

Dembrandt `--wcag` could not run in this environment. No WCAG pass/fail claims are made. Recommended checks before release: contrast on muted dark text, focus-visible states on nodes/nav, reduced-motion mode for shader pulses, keyboard activation of crystal nodes, semantic headings, and accessible text alternatives for WebGL.
